{
  "name": "jdm-portal-static",
  "version": "1.0.0",
  "description": "Minimal HTML dashboard for DevOps demonstration",
  "scripts": {
    "start": "serve -s . -l $PORT",
    "dev": "serve -s . -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}